<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css" type="text/css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <title>Manager | Follow</title>
</head>

<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>

    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container-fluid">
            <h3>
                Follow Administrator
            </h3>

            <ul class="nav nav-tabs">
                <li class="nav-item">
                    <button class="btn btn-outline-primary btn-block">
                        <a href="/"><i class="bi bi-arrow-left"></i> Back</a>
                    </button>&nbsp;
                    <div class="btn-group" role="group" aria-label="Basic radio toggle button group">
                        <input type="radio" class="btn-check" name="btnradio" id="btn-news" autocomplete="off"
                            value="true" checked>
                        <label class="btn btn-outline-primary" for="btn-news"><i class="bi bi-newspaper"></i></label>

                        <input type="radio" class="btn-check" name="btnradio" id="btn-ranking" autocomplete="off"
                            value="false">
                        <label class="btn btn-outline-primary" for="btn-ranking"><i
                                class="bi bi-graph-up"></i></label>
                    </div>
                </li>
                <li class="nav-item">
                    <div class="nav-link disabled">
                        <i class="bi bi-person"></i>&nbsp;<%= username %>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
    <div id="main">
    </div>
    <script>
        $(function () {
            $.ajax({
                type: 'GET',
                url: '/admin/news',
                success: function (result) {
                    $('#main').html(result);
                }
            });
            $("#btn-news").on("click", function () {
                $.ajax({
                    type: 'GET',
                    url: '/admin/news',
                    success: function (result) {
                        $('#main').html(result);
                    }
                });

            })
            $("#btn-ranking").on("click", function () {
                $.ajax({
                    type: 'GET',
                    url: '/admin/ranking',
                    success: function (result) {
                        $('#main').html(result);
                    }
                });

            })

        });
    </script>
</body>

</html>